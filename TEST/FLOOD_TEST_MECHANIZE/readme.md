This is a small script to run a stress test

- Requirements:
    multi-mechanize
    requests
    matplotlib
    ```bash
    pip install multi-mechanize
    pip install matplotlib
    pip install requests
    ```
    
- Running the test:
```bash
    multimech-run FLOOD_TEST_MECHANIZE
```
